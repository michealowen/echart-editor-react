## 修复计划

### 1. 修复代码编辑区的滚动问题
**修改文件：** `src/components/EditorPanel.css`
- 将 `.code-editor` 的 `overflow: hidden` 改为 `overflow: auto`
- 确保 `.cm-editor` 和 `.cm-scroller` 能够正确继承滚动行为
- 调整容器高度计算，确保滚动区域正确

### 2. 实现左右拖拽调整大小功能
**修改文件：** 
- `src/App.tsx` - 添加拖拽状态管理
- `src/App.css` - 改用 flex 布局替代 float 布局，添加分隔条样式
- `src/components/EditorPanel.css` - 移除 float 布局，改为 flex 子元素
- `src/components/ChartPanel.css` - 移除 float 布局，改为 flex 子元素

**实现步骤：**
- 在 `App.tsx` 中添加 `leftWidth` state（默认 40%）和拖拽相关状态
- 在左右面板之间添加一个可拖拽的分隔条组件
- 实现拖拽逻辑：监听鼠标事件，动态调整左右面板宽度
- 添加拖拽时的视觉反馈（光标样式、分隔条高亮等）
- 确保拖拽时图表能够自适应调整大小（调用 echarts 的 resize 方法）